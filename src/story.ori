// Story template converts a markdown input document to HTML
(story, filename) =>
  {
    // Remove the extension from the file name to get the title.
    title: @basename(filename)
    @text: story/@text
  }
  → @mdHtml
  → page.ori

# Table of Contents template: reduces the entire folder tree of stories to a
# table of contents, with headings for chapter folders and links to each story.
@map({ keyName: "chapter", valueMap: =`
  <h2>{{ chapter }}</h2>
  <ul>
    {{ @map(_, { keyName: "story", needsSourceValue: @false, valueMap: =`
    <li>
      <a href="/stories/{{ chapter }}/{{ story }}">{{ @basename(story) }}</a>
    </li>
    ` }) }}
  </ul>
` })

{
  fragments = @map(content, { deep: @true, extensions: "md→html", valueMap: @mdHtml })
  stories = @map(fragments, { deep: @true, valueMap: page.ori })

  public = @tree/merge(
    {
      assets
      index.html = index.ori(fragments)
    },
    stories
  )
}

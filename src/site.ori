# This file defines the structure of the entire site.
{
  # Stylesheet and fonts
  assets

  # Home page: lists all the stories grouped by folder
  index.html = index.ori(stories)

  # Indexes and resources for searching
  pagefind = package:@weborigami/pagefind({ stories })
  search.html = search.ori()

  # Stories area applies a page template to each markdown story
  stories = @mapDeep(content, { extensions: "md→html", valueMap: story.ori })
}

# The home page is mostly the table of contents.
# We also add the information from the ReadMe file.
=page.ori({
  title: "Cherokee Myths",
  @text: `
    {{ toc.ori(_) }}
    <hr>
    {{ @mdHtml(README.md) }}
  `
})
